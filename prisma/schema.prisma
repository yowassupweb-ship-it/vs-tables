// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum WorkMode {
  office
  remote
}

model Desk {
  id           String            @id
  label        String            @unique
  x            Int
  y            Int
  width        Int
  height       Int
  reservations DeskReservation[]
  dayModes      DeskDayMode[]
}

model DeskDayMode {
  id        String   @id @default(cuid())
  deskId    String
  dateKey   String
  workMode  WorkMode @default(office)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  desk Desk @relation(fields: [deskId], references: [id], onDelete: Cascade)

  @@unique([deskId, dateKey])
  @@index([deskId, dateKey])
}

model DeskReservation {
  id        String   @id @default(cuid())
  deskId    String
  userName  String
  note      String?
  workMode  WorkMode @default(office)
  startAt   DateTime @default(now())
  endAt     DateTime?
  createdAt DateTime @default(now())

  desk Desk @relation(fields: [deskId], references: [id], onDelete: Cascade)

  @@index([deskId, startAt])
  @@index([userName, startAt])
}
